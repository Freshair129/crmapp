# CL-20260322-001 — Employee Card Full Redesign + Task Board + RBAC Guard

**Version:** v1.5.2
**Date:** 2026-03-22
**Severity:** MINOR
**Tags:** #employee #ui #card #rbac #tasks #svgshape

---

## Summary

Redesign EmployeeManagement.js ทั้งหมด — folder-style dark glass card stack ตาม reference image, Task Board ใหม่, RBAC guard, auto-refresh JWT, และแก้ SVG folder shape จนตรงกับ reference (1 tab cut + 3 Q bezier rounded corners)

---

## Changes

### 1. Task Board (TaskPanel)
- `src/components/TaskPanel.js` — task board L0–L5 priority, urgentCount badge, create/edit modal
- `src/app/api/tasks/route.js` + `[id]/route.js` — GET+POST / PATCH+DELETE (soft delete)
- `src/lib/taskConstants.js` — NEW: extract constants ออกจาก route file (fix Vercel build error)
- `src/components/Sidebar.js` — tasks item + urgentTaskCount badge

### 2. RBAC Guard + Auth Fix
- `src/components/EmployeeManagement.js` — `canManage = can(role, 'system', 'view')`, ROLE_META uppercase keys
- `src/lib/authOptions.js` — JWT re-read from DB every 5 min (stale session fix)
- `src/app/api` middleware — /api/employees lowered from MANAGER → ADMIN level
- Page.js — scroll reset on view change (mainScrollRef)

### 3. Employee Card Full Redesign (EmployeeCardDeck)
- **ThumbnailStrip** — replace DotPager + flat row → centered wheel carousel (ResizeObserver + Framer Motion spring)
  - Active: scale 1.12, role-color glow shadow, name label
  - Gold highlight ring fixed at center
- **Card structure** — SVG dark glass (opacity 0.92) + role-color tint + glow border (active)
- **Top row** — circular avatar left + name / role / employeeId inline right
- **KpiBlock** — Revenue / Customers / CloseRate grid + Sparkline SVG (6-month, role-color area fill + glow)
- **StatusToggle bare** — emerald green active / grey inactive, one-click PATCH
- **Priority bar** — horizontal progress bar (renamed from "Permission"), glowing level circle L0–L5
- **Effects** — 4 animated smoke/haze divs (bottom bloom, left/right wisps, top ambient)
- **FAB** — role-color gradient, ArrowUpRight → syncs activeIndex + scrolls to detail panel
- **Contact pills** — phone / email / facebook

### 4. Employee ID Fix
- DB migration: `TVS-EMP-2026-XXXX` → `TVS-EMP-XXXX` (4 rows via Supabase SQL)
- `src/app/api/employees/route.js` — serial parser: use last segment (split at last `-`)
- `src/components/AdminPerformance.js` — fix hardcoded placeholder `TVS-EMP-2026-0001` → `TVS-EMP-0001`

### 5. SVG Folder Shape Iterations → Final Fix
Shape iterations (commits on 2026-03-22):

| Iteration | Path | Reason |
|---|---|---|
| v1 — initial notch | `L372 50 L322 0` (50px tab) | First glass card |
| v2 — larger notch | `L372 150 L222 0` (150px tab) | Boss: bigger |
| v3 — 3-cut corners | 3 diagonal cuts (octagon-like) per Boss spec A:372 B:372 C:322 D:100 E:50 F:272 | Boss-requested |
| **v4 — FINAL** | `M 28 0 Q 0 0 0 28 L 0 344 Q 0 372 28 372 L 344 372 Q 372 372 372 344 L 372 100 L 322 0 Z` | Boss: "ไม่เหมือน reference" → revert to 1 tab + 3 Q bezier rounded (R=28) |

**Final path (HEAD):**
```
M 28 0 Q 0 0 0 28          ← top-left rounded R=28
L 0 344 Q 0 372 28 372      ← left side + bottom-left rounded
L 344 372 Q 372 372 372 344 ← bottom + bottom-right rounded
L 372 100 L 322 0 Z         ← right side + folder tab diagonal
```
viewBox `0 0 372 372`, shimmer only on tab edge (`x1="323" y1="1" x2="371" y2="99"`)

---

## Files Changed

- `src/components/EmployeeManagement.js` — ThumbnailStrip, EmployeeCardDeck, KpiBlock, StatusToggle bare, SVG card
- `src/lib/taskConstants.js` — NEW
- `src/app/api/tasks/route.js`, `tasks/[id]/route.js` — NEW + fix named exports
- `src/components/Sidebar.js` — tasks badge
- `src/lib/authOptions.js` — JWT 5-min refresh
- `src/app/api/employees/route.js` — serial parser fix
- `src/components/AdminPerformance.js` — EMP ID placeholder fix

---

## Known Gotchas

- SVG `preserveAspectRatio="none"` — scales shape to any card width
- Folder tab position: `C=322, D=100` — tab starts at x=322 (top edge), goes to y=100 (right edge)
- All 4 SVG path layers (base fill, tint, glow, border) use the same path string — update all 4 if changing shape
