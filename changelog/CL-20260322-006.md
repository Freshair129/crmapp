# [CL-20260322-006] — Meta Ads Domain in MCP (v1.8.0) + adsOptimizeRepo Bug Fix

**Version:** v1.8.0
**Date:** 2026-03-22
**Severity:** MINOR
**Tags:** #mcp #ads #marketing #bugfix #adsOptimize
**Author:** Claude

---

## Summary

เพิ่ม Meta Ads domain เข้าไปใน MCP server — 7 tools ใหม่ (5 read + 2 write) ครอบคลุม campaign/adset/ad insights, daily trend, marketing summary, pause/resume, และ set budget พร้อมแก้ bug critical ใน `adsOptimizeRepo.js`

---

## Bug Fix: adsOptimizeRepo.js (Critical)

### Root Cause
ทุกฟังก์ชัน (9 functions) เรียก `getPrisma()` โดยไม่มี `await` — `getPrisma` เป็น `async function` ที่ return `Promise<PrismaClient>` ไม่ใช่ PrismaClient โดยตรง ทำให้ `prisma` variable เป็น Promise object → crash ทันทีที่เรียก `prisma.auditLog.create()` หรือ method ใดๆ

อีก bug หนึ่งคือ import path: `@/lib/prisma` ซึ่งไฟล์นั้นไม่มีอยู่จริง — ควรเป็น `@/lib/db`

### Fix
- `import { getPrisma } from '@/lib/prisma'` → `import { getPrisma } from '@/lib/db'`
- เพิ่ม `await` ทุก `getPrisma()` call ทั้ง 9 จุด: lines 76, 120, 164, 208, 257, 298, 369, 431, 451

### Functions Fixed
`pauseResume`, `updateDailyBudget`, `updateBid`, `duplicateCampaign`, `createLifetimeBudgetRequest`, `approveLifetimeBudgetRequest`, `rejectLifetimeBudgetRequest`, `getPendingRequests`, `getRequestById`

---

## New: Meta Ads Tools in MCP (7 tools)

### Read Tools (via marketingRepo)

| Tool | Handler | Description |
|---|---|---|
| `ads.get_campaign_insights` | `marketingRepo.getCampaignsWithAggregatedMetrics()` | spend/impressions/clicks/ROAS/CTR ต่อ campaign |
| `ads.get_adset_insights` | `marketingRepo.getAdSetsWithAggregatedMetrics()` | เหมือนกัน แยกตาม adset |
| `ads.get_ad_performance` | `marketingRepo.getAdsWithMetrics()` | performance ระดับ ad รายชิ้น |
| `ads.get_daily_metrics` | `marketingRepo.getDailyAggregatedMetrics()` | daily trend ย้อนหลัง (default 30 วัน) |
| `ads.get_marketing_summary` | `marketingRepo.getMarketingInsights()` | ภาพรวม 30d + all-time |

**Range parameter** (shared): `today | last_7d | last_30d | this_month | last_month | (omit = all time)`

### Write Tools (via adsOptimizeRepo — requires bug fix above)

| Tool | Handler | Description |
|---|---|---|
| `ads.pause_resume` | `adsOptimizeRepo.pauseResume()` | เปลี่ยน status campaign/adset/ad บน Meta จริง ⚠️ |
| `ads.set_daily_budget` | `adsOptimizeRepo.updateDailyBudget()` | ปรับ daily budget adset (บาท) บน Meta จริง ⚠️ |

### Total Tools: 15 → 22

---

## Files Modified

- `src/lib/repositories/adsOptimizeRepo.js` — bug fix (import + 9× await)
- `src/mcp/vschool-mcp-server.js` — +2 imports, +7 tools, +7 handlers, version 1.6.0→1.8.0
- `src/app/api/mcp/route.js` — +2 imports, +7 tools (compact), +7 handlers, version 1.6.0→1.8.0

---

## Verification

```bash
# Health check — tools count ควรเป็น 22
curl https://crmapp-pi.vercel.app/api/mcp
# Expected: {"status":"ok","server":"vschool-crm-mcp","version":"1.8.0","tools":22,"transport":"streamable-http"}

# List all tools (requires MCP_SECRET if set)
curl -X POST https://crmapp-pi.vercel.app/api/mcp \
  -H "Authorization: Bearer <secret>" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/list","params":{}}'
# ควรเห็น ads.get_campaign_insights, ads.pause_resume ฯลฯ ใน tools array
```

---

## Known Gotchas

- **ads.pause_resume / ads.set_daily_budget**: write operations จริงบน Meta — ใช้แล้วเปลี่ยน status/budget ทันที ไม่มี dry-run
- **actorEmployeeId**: ต้องส่งมาทุกครั้งสำหรับ write tools — ใช้สำหรับ audit log (ไม่ใช่ Meta auth)
- **range parameter**: ใช้ค่า `last_7d` / `last_30d` ไม่ใช่ `7d` / `30d` — format มาจาก `getMarketingRangeFilter()`
